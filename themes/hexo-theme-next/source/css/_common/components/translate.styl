// 翻译按钮样式 - 与其他菜单按钮保持一致
.menu-item-translate {
  .translate-btn {
    // 继承通用菜单按钮样式
    border-radius: 2px;
    padding: 0 10px;
    transition-property: background;

    // 取消 hover 的灰色背景
    &:hover { background: transparent; }

    i {
      font-size: 16px;
      line-height: 20px;
    }
  }
} 